/**
 * if variable is not nullish returns true, otherwise false
 * @param variable @type any
 * @returns boolean
 */
export const isDefined = (variable: any): boolean => variable !== null && variable !== undefined;
