import { Socket } from './web-socket';
import { CloseEvent, Connect, Listen, MessageEvent, Options, Protocols } from './web-socket.definition';

export { CloseEvent, Connect, Listen, MessageEvent, Options, Protocols, Socket };
